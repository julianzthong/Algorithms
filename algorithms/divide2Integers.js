/**
 * @param {number} dividend
 * @param {number} divisor
 * @return {number}
 */
var divide = function(dividend, divisor) {
  var isNegative =
  var count = 0;

  while (dividend >= 0) {
    dividend -= divisor;
    count += 1;
  }
  return count;
};